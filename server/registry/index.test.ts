import { Context } from 'koa';

/**
 * This is an internal route, this cannot be accessed from outside.
 *
 * It generates a .tar.gz with the package.json and files for a sandbox
 */
export default function bundle(ctx: Context, sandbox) {

}
